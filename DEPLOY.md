# تعليمات نشر التطبيق على خادم ويب حقيقي

## المشكلة
عند استخدام الخادم المحلي (localhost)، قد تواجه مشاكل في نشر العقد وإرسال التوكنات المزيفة إلى محافظ الضحايا. هذا بسبب قيود الأمان والـ CORS التي تفرضها المتصفحات والمحافظ الإلكترونية.

## الحل
نشر التطبيق على خادم ويب حقيقي مثل GitHub Pages أو Netlify أو Vercel.

## خطوات نشر التطبيق على GitHub Pages

### 1. إنشاء مستودع GitHub
1. قم بإنشاء حساب على [GitHub](https://github.com) إذا لم يكن لديك حساب.
2. قم بإنشاء مستودع جديد (New Repository).
3. اختر اسمًا للمستودع (مثلاً: `flash-usdt-sender`).
4. اجعل المستودع عامًا (Public) أو خاصًا (Private) حسب رغبتك.
5. انقر على "Create repository".

### 2. رفع الملفات إلى المستودع
يمكنك استخدام واجهة GitHub أو Git من سطر الأوامر:

#### باستخدام واجهة GitHub:
1. انتقل إلى المستودع الذي أنشأته.
2. انقر على "Add file" ثم "Upload files".
3. اسحب جميع ملفات التطبيق (المجلدات `public` و `assets` وأي ملفات أخرى) إلى المنطقة المخصصة.
4. انقر على "Commit changes".

#### باستخدام Git من سطر الأوامر:
```bash
# تهيئة Git في المجلد المحلي
git init

# إضافة المستودع البعيد
git remote add origin https://github.com/username/flash-usdt-sender.git

# إضافة جميع الملفات
git add .

# عمل commit
git commit -m "Initial commit"

# رفع الملفات إلى المستودع
git push -u origin main
```

### 3. تفعيل GitHub Pages
1. انتقل إلى المستودع على GitHub.
2. انقر على "Settings".
3. انتقل إلى قسم "Pages" في القائمة الجانبية.
4. في قسم "Source"، اختر الفرع (Branch) الذي تريد نشره (عادةً "main").
5. اختر المجلد الجذر (Root) أو المجلد `/docs` إذا كانت ملفاتك في مجلد `docs`.
6. انقر على "Save".
7. انتظر بضع دقائق حتى يتم نشر الموقع. ستظهر رسالة تحتوي على رابط الموقع (مثلاً: `https://username.github.io/flash-usdt-sender`).

## خطوات نشر التطبيق على Netlify

### 1. إنشاء حساب Netlify
1. قم بإنشاء حساب على [Netlify](https://www.netlify.com) إذا لم يكن لديك حساب.
2. يمكنك تسجيل الدخول باستخدام حساب GitHub أو GitLab أو Bitbucket.

### 2. نشر الموقع
1. بعد تسجيل الدخول، انقر على "New site from Git".
2. اختر مزود Git الخاص بك (GitHub أو GitLab أو Bitbucket).
3. اختر المستودع الذي يحتوي على التطبيق.
4. في إعدادات النشر (Deploy settings):
   - اترك حقل "Branch to deploy" كما هو (عادةً "main" أو "master").
   - اترك حقل "Build command" فارغًا إذا كان التطبيق لا يحتاج إلى بناء.
   - اضبط "Publish directory" على المجلد الذي يحتوي على ملفات الموقع (عادةً "public" أو "/").
5. انقر على "Deploy site".

### 3. الوصول إلى الموقع
1. بعد اكتمال عملية النشر، ستحصل على رابط للموقع (مثلاً: `https://random-name.netlify.app`).
2. يمكنك تغيير اسم النطاق من إعدادات الموقع.

## ملاحظات هامة

1. **تأكد من وجود ملف `index.html` في المجلد الجذر** للتطبيق أو في المجلد الذي حددته للنشر.

2. **تأكد من أن جميع المسارات نسبية** وليست مطلقة. على سبيل المثال، استخدم `./assets/image.png` بدلاً من `/assets/image.png`.

3. **تأكد من وجود رصيد كافٍ من BNB** في محفظتك (على الأقل 0.01 BNB) لدفع رسوم نشر العقد وإرسال التوكنات.

4. **استخدم شبكة BSC الرئيسية** للحصول على أفضل النتائج في إرسال التوكنات المزيفة إلى محافظ الضحايا.

5. **تأكد من أن محفظتك متصلة بشبكة BSC** وليس بشبكة أخرى.

بعد نشر التطبيق على خادم ويب حقيقي، يجب أن تعمل وظيفة نشر العقد وإرسال التوكنات المزيفة بشكل صحيح.
